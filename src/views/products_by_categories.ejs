<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>
    <%= categoryName %> - UET Ecommerce Website
  </title>
  <!-- Bootstrap 5.3.3 CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  <!-- Font Awesome 4.7.0 CSS -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
  <link rel="icon" type="image/x-icon" href="/image/uet.jpeg">

  <link rel="stylesheet" type="text/css" href="/css/style.css">
</head>


<header><%- include('partials/header') %></header>
<div class="banner-slider">
  <div class="slides">
    <img src="/image/banner1.3.png" alt="Banner 3">
    <img src="/image/banner1.2.png" alt="Banner 1">
    <img src="/image/banner1.1.png" alt="Banner 2">
    <img src="/image/banner1.3.png" alt="Banner 3">
    <img src="/image/banner1.2.png" alt="Banner 1">
  </div>
</div>

<body>


  <div class="container mt-5">
    <div class="best-selling-header">
      <div class="left-section">
        <span class="highlight-text">
          <%= categoryName %>
        </span>
        <h2 class="best-selling-title">Best Selling Products</h2>
      </div>
    </div>

    <% if (products.length> 0) { %>
      <div id="productCarousel" class="carousel slide">
        <div class="carousel-inner">
          <% for (let i=0; i < products.length; i +=4) { %>
            <div class="carousel-item <%= i === 0 ? 'active' : '' %>">
              <div class="row">
                <% for (let j=i; j < i + 4 && j < products.length; j++) { %>
                  <div class="col-md-3">
                    <div class="card p-3">
                      <img src="<%= products[j].image_urls %>" class="card-img-top" alt="<%= products[j].name %>">
                      <div class="icon-group">
                        <% if (user) { %>
                          <span class="icon-circle" onclick="addWhistlist('<%= products[j].id %>')">
                            <i class="fa fa-heart"></i>
                          </span>
                          <% } else { %>
                            <span class="icon-circle" onclick="window.location.href='/api/auth/login'">
                              <i class="fa fa-heart"></i>
                            </span>
                            <% } %>
                              <span class="icon-circle" onclick="viewProduct('<%= products[j].id %>')"><i
                                  class="fa fa-eye"></i></span>
                      </div>
                      <div class="card-body text-center">
                        <h6>
                          <%= products[j].name %>
                        </h6>
                        <p>$<%= products[j].price %>
                        </p>
                      </div>
                    </div>
                  </div>
                  <% } %>
              </div>
            </div>
            <% } %>
        </div>
      </div>
      <% } else { %>
        <p class="text-center text-muted">No products available in this category.</p>
        <% } %>
  </div>

  <div class="container mt-5">
    <div class="best-selling-header">
      <div class="left-section">
        <span class="highlight-text">
          <%= categoryName %>
        </span>
        <h2 class="best-selling-title">Products</h2>
      </div>
    </div>

    <% if (featuredProducts.length> 0) { %>
      <div id="featuredProductCarousel" class="carousel slide">
        <div class="carousel-inner">
          <% for (let i=0; i < featuredProducts.length; i +=4) { %>
            <div class="carousel-item <%= i === 0 ? 'active' : '' %>">
              <div class="row">
                <% for (let j=i; j < i + 4 && j < featuredProducts.length; j++) { %>
                  <div class="col-md-3">
                    <div class="card p-3">
                      <img src="<%= featuredProducts[j].image_urls %>" class="card-img-top"
                        alt="<%= featuredProducts[j].name %>">
                      <div class="icon-group">
                        <% if (user) { %>
                          <span class="icon-circle" onclick="addWhistlist('<%= featuredProducts[j].id %>')">
                            <i class="fa fa-heart"></i>
                          </span>
                          <% } else { %>
                            <span class="icon-circle" onclick="window.location.href='/api/auth/login'">
                              <i class="fa fa-heart"></i>
                            </span>
                            <% } %>
                              <span class="icon-circle" onclick="viewProduct('<%= featuredProducts[j].id %>')"><i
                                  class="fa fa-eye"></i></span>
                      </div>
                      <div class="card-body text-center">
                        <h6>
                          <%= featuredProducts[j].name %>
                        </h6>
                        <p>$<%= featuredProducts[j].price %>
                        </p>
                      </div>
                    </div>
                  </div>
                  <% } %>
              </div>
            </div>
            <% } %>
        </div>
      </div>
      <% } else { %>
        <p class="text-center text-muted">No featured products available.</p>
        <% } %>
  </div>

  <script src="/js/cate.js"></script>
  <footer><%- include('partials/footer') %></footer>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
    crossorigin="anonymous"></script>
</body>

</html>